<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ra2pi_beta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RA2PI_BETA">
        <activity android:name=".funcoes.activity_ListaPlanos"></activity>
        <activity android:name=".funcoes.PlanoQRCodeActivity" />
        <activity android:name=".funcoes.LoginQRCodeActivity" />
        <activity android:name=".funcoes.activity_NavegacaoVoz" />
        <activity android:name=".MainActivity" />
        <activity android:name=".funcoes.activity_tarefas" />
        <activity android:name=".PlayActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>